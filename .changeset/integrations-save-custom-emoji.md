---
"@plotday/twister": minor
---

Added: `Integrations.saveCustomEmoji(emoji: NewCustomEmoji[])` — connectors can populate Plot's custom-emoji cache so workspace emoji (e.g. Slack `:party_parrot:`) render as images and round-trip as reactions. New `NewCustomEmoji` type exported.
