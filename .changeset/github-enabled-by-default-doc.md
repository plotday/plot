---
"@plotday/twister": patch
---

Changed: correct the `Channel.enabledByDefault` doc example — GitHub organizations no longer serve as an "excluded by default" example, since the GitHub connector now enables repositories by default.
