---
"@plotday/twister": minor
---

Added: `LinkTypeConfig.sourceName` — a per-link-type product name that overrides the connector's display name when building "{source} {type}" copy. Lets aggregate connectors (e.g. the Google connector, display name "Gmail & Calendar") show the right per-product brand: "Google Calendar event", "Gmail thread", "Google Tasks task".
