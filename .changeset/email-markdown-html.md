---
"@plotday/twister": minor
---

Added: `markdownToHtml` (`@plotday/twister/utils/markdown-html`) — renders Plot Markdown note content to an HTML email fragment (mentions become `@Name`, GFM with `breaks`). Used by email connectors to send rich HTML bodies instead of raw Markdown as `text/plain`.
