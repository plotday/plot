---
"@plotday/twister": minor
---

Added: `NewLink.autoThread` ({ key, mode }) and `Connector.autoThreading` / `autoThreadingByDefault` for opt-in sequential auto-threading — the runtime folds a conversation that arrives as separate top-level messages into one thread.
