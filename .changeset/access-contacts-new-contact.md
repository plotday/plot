---
"@plotday/twister": minor
---

Changed: `accessContacts` accepts `NewContact[]` (email-based contacts) on write types, returns `Contact[]` on read types. Added `Contact` type for resolved contact identity.
