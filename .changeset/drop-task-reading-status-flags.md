---
"@plotday/twister": minor
---

Removed: `task` and `toRead` link-status flags. The task list and reading list features they fed have been dropped from Plot. The `todo` status flag is unchanged and still marks the connector's to-do state for agenda restore.
