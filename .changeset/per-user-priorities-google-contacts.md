---
"@plotday/connector-google-contacts": patch
---

Changed: README example updated to drop the `priority` argument from `activate()` and the `priorityId` context, reflecting the workspace-level Twister SDK.
