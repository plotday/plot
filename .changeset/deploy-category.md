---
"@plotday/twister": minor
---

Added: `plot deploy` now reads the `category` field from a connector's package.json and sends it to the API, so connectors are grouped into Messaging/Calendars sections in onboarding.
