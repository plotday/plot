---
"@plotday/twister": patch
---

Changed: Enhanced JSDoc on `this.run()` (Twist/Tool), `onChannelEnabled` (Connector) to clarify when to use `this.run()` vs `this.runTask()` and warn about blocking the HTTP response in lifecycle methods.
