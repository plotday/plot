---
"@plotday/twister": minor
---

Added: `Integrations.setInitialSyncing(channelId, syncing)` for connectors to signal initial bulk sync state per channel. Surfaces to the Flutter app via the new `user.twist_connection` view.
