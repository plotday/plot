---
"@plotday/twister": minor
---

Added: `Connector.fixedChannels` flag. When `true`, the connector's channel set is a fixed, enumerated list (no channels ever appear dynamically), and the connect/edit modal hides the per-connection "Sync new channels" toggle.
